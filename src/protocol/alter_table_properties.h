/*
 *  This file was autogenerated by the GPUdb schema processor.
 *
 *  DO NOT EDIT DIRECTLY.
 */
#ifndef __ALTER_TABLE_PROPERTIES_H__
#define __ALTER_TABLE_PROPERTIES_H__

namespace gpudb
{

    /**
     * A set of input parameters for {@link
     * #alterTableProperties(const AlterTablePropertiesRequest&) const}.
     * <p>
     * Updates properties for a group of specified tables. The user can change
     * the protected-ness of the tables and allow or disallow duplicate child
     * tables if they are all collections.
     */
    struct AlterTablePropertiesRequest
    {

        /**
         * Constructs an AlterTablePropertiesRequest object with default
         * parameter values.
         */
        AlterTablePropertiesRequest() :
            tableNames(std::vector<std::string>()),
            propertiesMap(std::map<std::string, std::string>()),
            options(std::map<std::string, std::string>())
        {
        }

        /**
         * Constructs an AlterTablePropertiesRequest object with the specified
         * parameters.
         * 
         * @param[in] tableNames  Names of the tables whose properties will be
         *                        updated. All provided must exist in GPUdb or
         *                        an error will be returned.
         * @param[in] propertiesMap  Map containing the properties of the
         *                           tables to be updated. Only one map can be
         *                           specified per function call so the changes
         *                           to the tables will be identical.  Default
         *                           value is an empty std::map.
         * @param[in] options  Optional parameters.  Default value is an empty
         *                     std::map.
         * 
         */
        AlterTablePropertiesRequest(const std::vector<std::string>& tableNames, const std::map<std::string, std::string>& propertiesMap, const std::map<std::string, std::string>& options):
            tableNames(tableNames),
            propertiesMap(propertiesMap),
            options(options)
        {
        }

        std::vector<std::string> tableNames;
        std::map<std::string, std::string> propertiesMap;
        std::map<std::string, std::string> options;
    };
}

namespace avro
{
    template<> struct codec_traits<gpudb::AlterTablePropertiesRequest>
    {
        static void encode(Encoder& e, const gpudb::AlterTablePropertiesRequest& v)
        {
            ::avro::encode(e, v.tableNames);
            ::avro::encode(e, v.propertiesMap);
            ::avro::encode(e, v.options);
        }

        static void decode(Decoder& d, gpudb::AlterTablePropertiesRequest& v)
        {
            if (::avro::ResolvingDecoder *rd = dynamic_cast< ::avro::ResolvingDecoder*>(&d))
            {
                const std::vector<size_t> fo = rd->fieldOrder();

                for (std::vector<size_t>::const_iterator it = fo.begin(); it != fo.end(); ++it)
                {
                    switch (*it)
                    {
                        case 0:
                            ::avro::decode(d, v.tableNames);
                            break;

                        case 1:
                            ::avro::decode(d, v.propertiesMap);
                            break;

                        case 2:
                            ::avro::decode(d, v.options);
                            break;

                        default:
                            break;
                    }
                }
            }
            else
            {
                ::avro::decode(d, v.tableNames);
                ::avro::decode(d, v.propertiesMap);
                ::avro::decode(d, v.options);
            }
        }
    };
}

namespace gpudb
{

    /**
     * A set of output parameters for {@link
     * #alterTableProperties(const AlterTablePropertiesRequest&) const}.
     * <p>
     * Updates properties for a group of specified tables. The user can change
     * the protected-ness of the tables and allow or disallow duplicate child
     * tables if they are all collections.
     */
    struct AlterTablePropertiesResponse
    {

        /**
         * Constructs an AlterTablePropertiesResponse object with default
         * parameter values.
         */
        AlterTablePropertiesResponse() :
            tableNames(std::vector<std::string>()),
            propertiesMap(std::map<std::string, std::string>())
        {
        }

        std::vector<std::string> tableNames;
        std::map<std::string, std::string> propertiesMap;
    };
}

namespace avro
{
    template<> struct codec_traits<gpudb::AlterTablePropertiesResponse>
    {
        static void encode(Encoder& e, const gpudb::AlterTablePropertiesResponse& v)
        {
            ::avro::encode(e, v.tableNames);
            ::avro::encode(e, v.propertiesMap);
        }

        static void decode(Decoder& d, gpudb::AlterTablePropertiesResponse& v)
        {
            if (::avro::ResolvingDecoder *rd = dynamic_cast< ::avro::ResolvingDecoder*>(&d))
            {
                const std::vector<size_t> fo = rd->fieldOrder();

                for (std::vector<size_t>::const_iterator it = fo.begin(); it != fo.end(); ++it)
                {
                    switch (*it)
                    {
                        case 0:
                            ::avro::decode(d, v.tableNames);
                            break;

                        case 1:
                            ::avro::decode(d, v.propertiesMap);
                            break;

                        default:
                            break;
                    }
                }
            }
            else
            {
                ::avro::decode(d, v.tableNames);
                ::avro::decode(d, v.propertiesMap);
            }
        }
    };
}

#endif
