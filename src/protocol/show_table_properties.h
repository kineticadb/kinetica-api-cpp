/*
 *  This file was autogenerated by the GPUdb schema processor.
 *
 *  DO NOT EDIT DIRECTLY.
 */
#ifndef __SHOW_TABLE_PROPERTIES_H__
#define __SHOW_TABLE_PROPERTIES_H__

namespace gpudb
{

    /**
     * A set of input parameters for {@link
     * #showTableProperties(ShowTablePropertiesRequest&)}.
     * <p>
     * Retrieves some table properties for each of the specified tables. For
     * each valid table, it returns whether it is a protected table and also if
     * it allows duplicate child tables if it happens to be a collection.
     */
    struct ShowTablePropertiesRequest
    {

        /**
         * Constructs a ShowTablePropertiesRequest object with default
         * parameter values.
         */
        ShowTablePropertiesRequest() :
            tableNames(std::vector<std::string>()),
            options(std::map<std::string, std::string>())
        {
        }

        /**
         * Constructs a ShowTablePropertiesRequest object with the specified
         * parameters.
         * 
         * @param[in] tableNames  Tables whose properties will be fetched. All
         *                        provided tables must exist in GPUdb, or GPUdb
         *                        returns an error.
         * @param[in] options  Optional parameters.  Default value is an empty
         *                     {@link std::map}.
         * 
         */
        ShowTablePropertiesRequest(const std::vector<std::string>& tableNames, const std::map<std::string, std::string>& options):
            tableNames(tableNames),
            options(options)
        {
        }

        std::vector<std::string> tableNames;
        std::map<std::string, std::string> options;
    };
}

namespace avro
{
    template<> struct codec_traits<gpudb::ShowTablePropertiesRequest>
    {
        static void encode(Encoder& e, const gpudb::ShowTablePropertiesRequest& v)
        {
            ::avro::encode(e, v.tableNames);
            ::avro::encode(e, v.options);
        }

        static void decode(Decoder& d, gpudb::ShowTablePropertiesRequest& v)
        {
            if (::avro::ResolvingDecoder *rd = dynamic_cast< ::avro::ResolvingDecoder*>(&d))
            {
                const std::vector<size_t> fo = rd->fieldOrder();

                for (std::vector<size_t>::const_iterator it = fo.begin(); it != fo.end(); ++it)
                {
                    switch (*it)
                    {
                        case 0:
                            ::avro::decode(d, v.tableNames);
                            break;

                        case 1:
                            ::avro::decode(d, v.options);
                            break;

                        default:
                            break;
                    }
                }
            }
            else
            {
                ::avro::decode(d, v.tableNames);
                ::avro::decode(d, v.options);
            }
        }
    };
}

namespace gpudb
{

    /**
     * A set of output parameters for {@link
     * #showTableProperties(ShowTablePropertiesRequest&)}.
     * <p>
     * Retrieves some table properties for each of the specified tables. For
     * each valid table, it returns whether it is a protected table and also if
     * it allows duplicate child tables if it happens to be a collection.
     */
    struct ShowTablePropertiesResponse
    {

        /**
         * Constructs a ShowTablePropertiesResponse object with default
         * parameter values.
         */
        ShowTablePropertiesResponse() :
            tableNames(std::vector<std::string>()),
            propertiesMaps(std::vector<std::map<std::string, std::string> >())
        {
        }

        std::vector<std::string> tableNames;
        std::vector<std::map<std::string, std::string> > propertiesMaps;
    };
}

namespace avro
{
    template<> struct codec_traits<gpudb::ShowTablePropertiesResponse>
    {
        static void encode(Encoder& e, const gpudb::ShowTablePropertiesResponse& v)
        {
            ::avro::encode(e, v.tableNames);
            ::avro::encode(e, v.propertiesMaps);
        }

        static void decode(Decoder& d, gpudb::ShowTablePropertiesResponse& v)
        {
            if (::avro::ResolvingDecoder *rd = dynamic_cast< ::avro::ResolvingDecoder*>(&d))
            {
                const std::vector<size_t> fo = rd->fieldOrder();

                for (std::vector<size_t>::const_iterator it = fo.begin(); it != fo.end(); ++it)
                {
                    switch (*it)
                    {
                        case 0:
                            ::avro::decode(d, v.tableNames);
                            break;

                        case 1:
                            ::avro::decode(d, v.propertiesMaps);
                            break;

                        default:
                            break;
                    }
                }
            }
            else
            {
                ::avro::decode(d, v.tableNames);
                ::avro::decode(d, v.propertiesMaps);
            }
        }
    };
}

#endif
